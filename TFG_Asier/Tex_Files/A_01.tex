\hiddenappendixchapter
    {Evolution of the Inherited Inspection Modules}
    {app:inherited_interface_evolution}
\label{app:application_interface_overview}

\providecommand{\appAimage}[3]{%
    \IfFileExists{#1}{%
        \includegraphics[width=#2,height=#3,keepaspectratio]{#1}%
    }{%
        \fbox{\parbox[c][0.15\textheight][c]{0.90\linewidth}{%
            \centering\textbf{Capture pending}\\[0.4em]
            \texttt{\detokenize{#1}}}}%
    }%
}

\begingroup
\setlength{\textfloatsep}{8pt plus 2pt minus 2pt}
\setlength{\floatsep}{8pt plus 2pt minus 2pt}
\setlength{\intextsep}{8pt plus 2pt minus 2pt}
\captionsetup{skip=4pt}

The inherited inspection modules retain their original purpose while using the
shared hierarchy, case collection and transient controls of the final
application. Equivalent physical selections are shown whenever both versions
are compared. The differences therefore correspond to the interface and its
operating logic rather than to unrelated thermal results.

The comparison follows the order of the common tabs. Each block first identifies
the original inspection task and then shows how the same task is integrated into
the transient and multi-case workflow. The figures are intentionally presented
at a large scale because the location of the selectors, case controls and result
areas forms part of the comparison.

\section{Flow Explorer}
\label{appsec:flow_explorer_evolution}

The original Flow Explorer processes one active dataset and provides a direct
source--destination heat-flow query. The source group, destination group,
thermal quantity and unit are selected before the result is generated. This
arrangement is sufficient when only one steady-state dataset is available,
because neither the case identity nor the output instant needs to be selected
explicitly.

\begin{figure}[H]
    \centering
    \appAimage{Figures/Anexo_01/original_flow_explorer.png}
        {1.0\textwidth}{0.44\textheight}
    \caption{Original Flow Explorer interface.}
    \label{fig:appendix_original_flow_explorer}
\end{figure}

The original layout already defines the physical workflow retained by the final
application: a source element is related to a destination element and the
corresponding exchanged quantity is represented.

The developed Flow Explorer retains this sequence while making the complete
analysis context visible before the result is generated. Several compatible cases can be selected, transient
results are evaluated at a chosen output time and the representation can show
either the absolute values or the difference between a reference case and one
or more comparison cases.

\begin{figure}[H]
    \centering
    \appAimage{Figures/Anexo_01/final_flow_explorer.png}
        {1.0\textwidth}{0.44\textheight}
    \caption{Developed Flow Explorer interface with transient and multi-case
    controls.}
    \label{fig:appendix_final_flow_explorer}
\end{figure}

The model hierarchy is also used to order the available groups and to remove
redundant descendants when a parent branch is already selected. This prevents
the same part of the model from being included twice in an aggregated result.
The generated representation can subsequently be exported or stored together
with the case set, physical selection, units, time and graphical options.

Although the developed tab contains more controls, the original
source--destination structure remains recognisable. Case management and
transient evaluation are placed around the inherited query rather than being
implemented as a separate analysis procedure.

\section{Case Information}
\label{appsec:case_information_evolution}

The original \textit{Model info} tab reports the structural and coupling data
of the active result file. The developed \textit{Case info} tab retains these
summaries for every loaded case, preserves the visible case name and adds
level-based coupling inspection at a selected output time.

\begin{figure}[H]
    \centering
    \textbf{Original version}\par\vspace{0.5em}
    \appAimage{Figures/Anexo_01/original_model_info.png}
        {1.0\textwidth}{0.35\textheight}

    \vspace{1em}
    \textbf{Developed version}\par\vspace{0.5em}
    \appAimage{Figures/Anexo_01/final_case_info.png}
        {1.0\textwidth}{0.35\textheight}

    \caption{Evolution from active-model information to case-specific
    inspection.}
    \label{fig:appendix_case_information_evolution}
\end{figure}

Both versions preserve access to the model size, hierarchy levels and coupling
summaries. The final interface removes the dependence on a single active file:
the requested information is associated with the selected case, while the
common hierarchy continues to provide the engineering structure used to
interpret the identifiers. The temporal selector is only relevant when the
reported values depend on the transient output instant.

\section{Group Attributes}
\label{appsec:group_attributes_evolution}

The original tab obtains the requested group attributes from one active case
and, for transient datasets, from the first available result row. The developed
module separates case selection, group selection and attribute selection, then
evaluates the request at the chosen output time. When several cases are active,
the same physical and numerical selection is applied to each of them.

\begin{figure}[H]
    \centering
    \textbf{Original version}\par\vspace{0.5em}
    \appAimage{Figures/Anexo_01/original_group_attributes.png}
        {1.0\textwidth}{0.3\textheight}

    \vspace{1em}
    \textbf{Developed version}\par\vspace{0.5em}
    \appAimage{Figures/Anexo_01/final_group_attributes.png}
        {1.0\textwidth}{0.3\textheight}

    \caption{Evolution of the group-attribute inspection area.}
    \label{fig:appendix_group_attributes_evolution}
\end{figure}

The resulting tables remain case-specific, which avoids merging values that
belong to different simulations. A difference representation can instead be
requested explicitly when the compared attributes are compatible. The final
layout therefore preserves the original group-based inspection while making
the case and temporal basis of every table unambiguous.

\section{Linear and Radiative Conductors}
\label{appsec:conductor_modules_evolution}

The conductor inspection remains divided into linear and radiative matrices.
This separation is important because GL and GR describe different coupling
terms even when they connect the same physical groups. The developed modules
apply a common source--target selection to the loaded cases and evaluate the
corresponding values at the selected output time.

\paragraph{Linear conductors.}
The GL tab retains the original source and destination selectors while adding
case selection, explicit time control and a signed source--target table. The
number of detected couplings and their total conductance are reported together
with the detailed entries.

\begin{figure}[H]
    \centering
    \textbf{Original GL module}\par\vspace{0.5em}
    \appAimage{Figures/Anexo_01/original_gl_conductors.png}
        {1.0\textwidth}{0.3\textheight}

    \vspace{1em}
    \textbf{Developed GL module}\par\vspace{0.5em}
    \appAimage{Figures/Anexo_01/final_gl_conductors.png}
        {1.0\textwidth}{0.3\textheight}

    \caption{Evolution of the linear-conductor inspection area.}
    \label{fig:appendix_gl_conductors_evolution}
\end{figure}

The table can be exported and its selection can be restored later. These
functions do not change the underlying GL data; they preserve the context
required to reproduce the same inspection for one or several cases.

\paragraph{Radiative conductors.}
The GR module follows the same organisation, but the radiative coefficients
remain independent from the linear conductances. The common arrangement allows
the user to move between GL and GR inspection without redefining the case,
hierarchy or temporal context.

\begin{figure}[H]
    \centering
    \textbf{Original GR module}\par\vspace{0.2em}
    \appAimage{Figures/Anexo_01/original_gr_conductors.png}
        {1.0\textwidth}{0.3\textheight}

    \vspace{1em}
    \textbf{Developed GR module}\par\vspace{0.2em}
    \appAimage{Figures/Anexo_01/final_gr_conductors.png}
        {1.0\textwidth}{0.3\textheight}

    \caption{Evolution of the radiative-conductor inspection area.}
    \label{fig:appendix_gr_conductors_evolution}
\end{figure}

The final GL and GR tabs therefore provide parallel procedures while preserving
the physical distinction between both matrices. Each exported table identifies
the corresponding coupling type and the selected direction between groups.

\section{Nodes and Group Study}
\label{appsec:nodes_group_study_evolution}

The nodal table retains attribute and group selection while adding an explicit
filter action, output-time selection and case-specific or difference tables.
The filtering controls are separated from the imported result table, so the
visible subset can be changed without modifying the stored data.

\begin{figure}[H]
    \centering
    \textbf{Original Nodes module}\par\vspace{0.2em}
    \appAimage{Figures/Anexo_01/original_nodes.png}
        {1.0\textwidth}{0.3\textheight}

    \vspace{1em}
    \textbf{Developed Nodes module}\par\vspace{0.2em}
    \appAimage{Figures/Anexo_01/final_nodes.png}
        {1.0\textwidth}{0.3\textheight}

    \caption{Evolution of the nodal-inspection interface.}
    \label{fig:appendix_nodes_evolution}
\end{figure}

The developed version can apply the same nodal request to several compatible
cases and can export the resulting table. The explicit application of the
filter also makes the visible selection reproducible when the analysis is
restored.

\clearpage

The \textit{Group study} tab combines a component summary with the inspection
of internal and external GL and GR connections. The original interface already
organises these operations around one selected group. The developed version
retains this component-centred structure while introducing the shared case,
time and export controls used by the rest of the application.

\begin{figure}[H]
    \centering
    \textbf{Original Group Study module}\par\vspace{0.2em}
    \appAimage{Figures/Anexo_01/original_group_study.png}
        {1.0\textwidth}{0.3\textheight}

    \vspace{0.2em}
    \textbf{Developed Group Study module}\par\vspace{0.2em}
    \appAimage{Figures/Anexo_01/final_group_study.png}
        {1.0\textwidth}{0.3\textheight}

    \caption{Evolution of the group-study interface.}
    \label{fig:appendix_group_study_evolution}
\end{figure}

The component summary and the conductor analyses can be generated and restored
independently. They nevertheless use the same loaded cases and hierarchy, which
keeps the selected component consistent throughout the complete inspection.

\section{Summary of the Inherited Modules}
\label{appsec:inherited_modules_summary}

Table~\ref{tab:appendix_inherited_module_summary} condenses the adaptations
shown in the preceding figures. The initial inspection functions remain
available, while the final implementation extends their case organisation,
transient evaluation, selection consistency and reproducibility. The practical
effect column distinguishes the visible interface change from its consequence
during repeated analysis.

\input{Tables/table_appendix_inherited_module_summary.tex}

The inherited inspection scope is therefore retained within a common transient
and multi-case workflow, allowing equivalent selections to be applied,
compared and restored consistently.

\endgroup
